/**
 * Fill the credentials with the saved form data
 * @param params {params}
 * @returns {boolean}
 */
export default function(params) {
    let login = params.login;

    if (!login.form_data) {
        return false;
    }

    // TODO
    console.log('TODO');


    // Not able to fill the form properly
    return false;
}
